//
//  UIColorExtension.swift
//  Hofu
//
//  Created by Muhammad Rydwan on 14/12/24.
//

import Foundation
import UIKit

extension UIColor {
    class var Warning25: UIColor {
        guard let color = UIColor(named: "Warning25") else {
            fatalError("Could not find color named 'Warning25'")
        }
        return color
    }
    class var Warning50: UIColor {
        guard let color = UIColor(named: "Warning50") else {
            fatalError("Could not find color named 'Warning50'")
        }
        return color
    }
    class var Warning100: UIColor {
        guard let color = UIColor(named: "Warning100") else {
            fatalError("Could not find color named 'Warning100'")
        }
        return color
    }
    class var Warning200: UIColor {
        guard let color = UIColor(named: "Warning200") else {
            fatalError("Could not find color named 'Warning200'")
        }
        return color
    }
    class var Warning300: UIColor {
        guard let color = UIColor(named: "Warning300") else {
            fatalError("Could not find color named 'Warning300'")
        }
        return color
    }
    class var Warning400: UIColor {
        guard let color = UIColor(named: "Warning400") else {
            fatalError("Could not find color named 'Warning400'")
        }
        return color
    }
    class var Warning500: UIColor {
        guard let color = UIColor(named: "Warning500") else {
            fatalError("Could not find color named 'Warning500'")
        }
        return color
    }
    class var Warning600: UIColor {
        guard let color = UIColor(named: "Warning600") else {
            fatalError("Could not find color named 'Warning600'")
        }
        return color
    }
    class var Warning700: UIColor {
        guard let color = UIColor(named: "Warning700") else {
            fatalError("Could not find color named 'Warning700'")
        }
        return color
    }
    class var Warning800: UIColor {
        guard let color = UIColor(named: "Warning800") else {
            fatalError("Could not find color named 'Warning800'")
        }
        return color
    }
    class var Warning900: UIColor {
        guard let color = UIColor(named: "Warning900") else {
            fatalError("Could not find color named 'Warning900'")
        }
        return color
    }
}
