//
//  UIColorExtension.swift
//  Hofu
//
//  Created by Muhammad Rydwan on 14/12/24.
//

import Foundation
import UIKit

extension UIColor {
    class var Success25: UIColor {
        guard let color = UIColor(named: "Success25") else {
            fatalError("Could not find color named 'Success25'")
        }
        return color
    }
    class var Success50: UIColor {
        guard let color = UIColor(named: "Success50") else {
            fatalError("Could not find color named 'Success50'")
        }
        return color
    }
    class var Success100: UIColor {
        guard let color = UIColor(named: "Success100") else {
            fatalError("Could not find color named 'Success100'")
        }
        return color
    }
    class var Success200: UIColor {
        guard let color = UIColor(named: "Success200") else {
            fatalError("Could not find color named 'Success200'")
        }
        return color
    }
    class var Success300: UIColor {
        guard let color = UIColor(named: "Success300") else {
            fatalError("Could not find color named 'Success300'")
        }
        return color
    }
    class var Success400: UIColor {
        guard let color = UIColor(named: "Success400") else {
            fatalError("Could not find color named 'Success400'")
        }
        return color
    }
    class var Success500: UIColor {
        guard let color = UIColor(named: "Success500") else {
            fatalError("Could not find color named 'Success500'")
        }
        return color
    }
    class var Success600: UIColor {
        guard let color = UIColor(named: "Success600") else {
            fatalError("Could not find color named 'Success600'")
        }
        return color
    }
    class var Success700: UIColor {
        guard let color = UIColor(named: "Success700") else {
            fatalError("Could not find color named 'Success700'")
        }
        return color
    }
    class var Success800: UIColor {
        guard let color = UIColor(named: "Success800") else {
            fatalError("Could not find color named 'Success800'")
        }
        return color
    }
    class var Success900: UIColor {
        guard let color = UIColor(named: "Success900") else {
            fatalError("Could not find color named 'Success900'")
        }
        return color
    }
}
