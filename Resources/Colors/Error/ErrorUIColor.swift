//
//  UIColorExtension.swift
//  Hofu
//
//  Created by Muhammad Rydwan on 14/12/24.
//

import Foundation
import UIKit

extension UIColor {
    class var Error25: UIColor {
        guard let color = UIColor(named: "Error25") else {
            fatalError("Could not find color named 'Error25'")
        }
        return color
    }
    class var Error50: UIColor {
        guard let color = UIColor(named: "Error50") else {
            fatalError("Could not find color named 'Error50'")
        }
        return color
    }
    class var Error100: UIColor {
        guard let color = UIColor(named: "Error100") else {
            fatalError("Could not find color named 'Error100'")
        }
        return color
    }
    class var Error200: UIColor {
        guard let color = UIColor(named: "Error200") else {
            fatalError("Could not find color named 'Error200'")
        }
        return color
    }
    class var Error300: UIColor {
        guard let color = UIColor(named: "Error300") else {
            fatalError("Could not find color named 'Error300'")
        }
        return color
    }
    class var Error400: UIColor {
        guard let color = UIColor(named: "Error400") else {
            fatalError("Could not find color named 'Error400'")
        }
        return color
    }
    class var Error500: UIColor {
        guard let color = UIColor(named: "Error500") else {
            fatalError("Could not find color named 'Error500'")
        }
        return color
    }
    class var Error600: UIColor {
        guard let color = UIColor(named: "Error600") else {
            fatalError("Could not find color named 'Error600'")
        }
        return color
    }
    class var Error700: UIColor {
        guard let color = UIColor(named: "Error700") else {
            fatalError("Could not find color named 'Error700'")
        }
        return color
    }
    class var Error800: UIColor {
        guard let color = UIColor(named: "Error800") else {
            fatalError("Could not find color named 'Error800'")
        }
        return color
    }
    class var Error900: UIColor {
        guard let color = UIColor(named: "Error900") else {
            fatalError("Could not find color named 'Error900'")
        }
        return color
    }
}
