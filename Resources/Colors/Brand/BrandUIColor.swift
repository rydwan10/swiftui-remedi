//
//  UIColorExtension.swift
//  Hofu
//
//  Created by Muhammad Rydwan on 14/12/24.
//

import Foundation
import UIKit

extension UIColor {
    class var Brand25: UIColor {
        guard let color = UIColor(named: "Brand25") else {
            fatalError("Could not find color named 'Brand25'")
        }
        return color
    }
    class var Brand50: UIColor {
        guard let color = UIColor(named: "Brand50") else {
            fatalError("Could not find color named 'Brand50'")
        }
        return color
    }
    class var Brand100: UIColor {
        guard let color = UIColor(named: "Brand100") else {
            fatalError("Could not find color named 'Brand100'")
        }
        return color
    }
    class var Brand200: UIColor {
        guard let color = UIColor(named: "Brand200") else {
            fatalError("Could not find color named 'Brand200'")
        }
        return color
    }
    class var Brand300: UIColor {
        guard let color = UIColor(named: "Brand300") else {
            fatalError("Could not find color named 'Brand300'")
        }
        return color
    }
    class var Brand400: UIColor {
        guard let color = UIColor(named: "Brand400") else {
            fatalError("Could not find color named 'Brand400'")
        }
        return color
    }
    class var Brand500: UIColor {
        guard let color = UIColor(named: "Brand500") else {
            fatalError("Could not find color named 'Brand500'")
        }
        return color
    }
    class var Brand600: UIColor {
        guard let color = UIColor(named: "Brand600") else {
            fatalError("Could not find color named 'Brand600'")
        }
        return color
    }
    class var Brand700: UIColor {
        guard let color = UIColor(named: "Brand700") else {
            fatalError("Could not find color named 'Brand700'")
        }
        return color
    }
    class var Brand800: UIColor {
        guard let color = UIColor(named: "Brand800") else {
            fatalError("Could not find color named 'Brand800'")
        }
        return color
    }
    class var Brand900: UIColor {
        guard let color = UIColor(named: "Brand900") else {
            fatalError("Could not find color named 'Brand900'")
        }
        return color
    }
}
