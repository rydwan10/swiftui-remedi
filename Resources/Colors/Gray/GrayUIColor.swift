//
//  UIColorExtension.swift
//  Hofu
//
//  Created by Muhammad Rydwan on 14/12/24.
//

import Foundation
import UIKit

extension UIColor {
    class var Gray25: UIColor {
        guard let color = UIColor(named: "Gray25") else {
            fatalError("Could not find color named 'Gray25'")
        }
        return color
    }
    class var Gray50: UIColor {
        guard let color = UIColor(named: "Gray50") else {
            fatalError("Could not find color named 'Gray50'")
        }
        return color
    }
    class var Gray100: UIColor {
        guard let color = UIColor(named: "Gray100") else {
            fatalError("Could not find color named 'Gray100'")
        }
        return color
    }
    class var Gray200: UIColor {
        guard let color = UIColor(named: "Gray200") else {
            fatalError("Could not find color named 'Gray200'")
        }
        return color
    }
    class var Gray300: UIColor {
        guard let color = UIColor(named: "Gray300") else {
            fatalError("Could not find color named 'Gray300'")
        }
        return color
    }
    class var Gray400: UIColor {
        guard let color = UIColor(named: "Gray400") else {
            fatalError("Could not find color named 'Gray400'")
        }
        return color
    }
    class var Gray500: UIColor {
        guard let color = UIColor(named: "Gray500") else {
            fatalError("Could not find color named 'Gray500'")
        }
        return color
    }
    class var Gray600: UIColor {
        guard let color = UIColor(named: "Gray600") else {
            fatalError("Could not find color named 'Gray600'")
        }
        return color
    }
    class var Gray700: UIColor {
        guard let color = UIColor(named: "Gray700") else {
            fatalError("Could not find color named 'Gray700'")
        }
        return color
    }
    class var Gray800: UIColor {
        guard let color = UIColor(named: "Gray800") else {
            fatalError("Could not find color named 'Gray800'")
        }
        return color
    }
    class var Gray900: UIColor {
        guard let color = UIColor(named: "Gray900") else {
            fatalError("Could not find color named 'Gray900'")
        }
        return color
    }
}
